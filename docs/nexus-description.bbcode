[b][size=5]Lumine (Genshin Impact) - Terran Female NPC Replacer[/size][/b]

把《原神》的女主角[b]荧[/b]替换成《X4：基石》里的[b]泰伦（Terran）女性[/b] NPC 外观。

采用 X4 标准的「换网格、留骨架」方式：使用原版共享骨架与全套共享动画，
不修改任何游戏原始文件。

[b]版本[/b]：1.0　　[b]游戏版本[/b]：X4: Foundations 9.00（需《人类的摇篮》DLC）

[line]

[b][size=4]功能[/size][/b]

[list]
[*]用荧的模型、贴图与材质替换泰伦女性 NPC 外观
[*]沿用原版骨架与共享动画，动作、注视、手持挂点全部正常
[*]覆盖[b]泰伦[/b]与[b]先锋[/b]两个势力的女性，并[b]包含剧情/任务 NPC[/b]
[*]不修改任何游戏文件，可随时启用/停用，存档安全
[*]NPC 的身份（名字、职称、背景、语音）不受影响，只有外观改变
[/list]

[line]

[b][size=4]安装[/size][/b]

[list=1]
[*]下载 [b]x4_lumine_mod_v1.0.zip[/b]
[*]解压到游戏根目录下的 [font=Courier New]extensions\[/font] 文件夹，得到：
[code]X4 Foundations\extensions\x4_lumine_mod\
    content.xml
    ext_01.cat
    ext_01.dat[/code]
[*]启动游戏 → 主菜单 [b]扩展[/b]（Extensions）→ 勾选 [b]Lumine (Genshin Impact)[/b]
[*]读取存档或开新档，前往泰伦星区（Segaris、火星、月面、Getsu Fune）或先锋空间站
[/list]

[b]提示[/b]：NPC 外观在生成时决定，已在场景里的 NPC 不会立刻变化。
移动到有泰伦女性的区域，或重新载入即可。

[line]

[b][size=4]替换范围[/size][/b]

所有[b]泰伦种族的女性[/b]，这是一个精确定义的集合，而不是"看起来像泰伦的"：

[list]
[*]泰伦势力与先锋势力的[b]全部外观池[/b]
[*]外加由任务脚本[b]直接引用、从不出现在任何外观池里[/b]的剧情角色
[/list]

受影响的 45 个 NPC macro 是按「沿继承链解析出的种族 / 性别 / 势力」筛出来的，
[b]不是按名字[/b] —— X4 的命名在两个方向上都会误导（详见工程日志）。

[b]不替换[/b]：阿贡（Argon）女性、Yaki 女性（她们是 Argon 种族）、以及玩家自己。
若想同时替换 Argon 女性，可与 Argon 替换 mod 一起使用，两者[b]互不冲突[/b]。

[line]

[b][size=4]已知缺陷与限制[/size][/b]

[b]安装前请务必阅读。[/b]

[b][size=3]1. 关节处的皮肤会撕裂（脚腕、肩臂、手肘）—— 最明显的问题[/size][/b]

脚腕处能看到靴口与腿错开一点；肩部与肘部有细缝。

[b]原因[/b]：这套模型的骨架把脊柱骨叠在一起（[font=Courier New]下半身[/font] 与
[font=Courier New]上半身[/font] 只差 0.7 cm），而 X4 的
[font=Courier New]Bip01 Spine[/font] 与 [font=Courier New]Spine1[/font] 相差 12.8 cm。
逐骨各对各的目标，就把源里几乎重合的两组顶点拉开 12.8 cm，蒙皮在两者之间混合，
于是跨越它们的每条边都被拉伸。实测：394 条边的拉伸比超过 2 倍。

[b]试过的修法[/b]：给每条骨链一个共享位移（就是已经用来修好脖子和脚部的同一个办法）。
数字上确实改善，但[b]手臂和脚腕会出现看得见的弯折[/b]，比它修掉的缝隙更显眼，因此已回退。
权重平滑也无效：4 / 10 / 20 / 40 轮结果完全相同。

彻底修好意味着改骨架比例，而骨架必须与 vanilla 逐字节相同，否则动画会坏。
所以这个问题[b]保留[/b]。近距离特写最明显，正常对话距离下仍可察觉。

[b][size=3]2. 手指不能单独活动[/size][/b]

手指绑在手掌上。这是为了换取正确的手型：源模型的手指是并拢建模的，而 X4 骨架是张开的，
把每根手指对到自己的骨上会把虎口撕开，像少了一块皮。

[b][size=3]3. 头发与头饰是跟头转的硬壳[/size][/b]

整块绑在头骨上，会随头转动，但没有摆动。X4 的 91 骨骼骨架里没有发丝链可绑。

[b][size=3]4. 裙摆与飘带没有布料模拟[/size][/b]

同样原因 —— 整条链锚定在脊柱上。它们保持形状并跟随身体，但走路时不会摆动。

[b][size=3]5. 眼球不跟随注视[/size][/b]

固定在头上，而不是由 X4 的 look-at 系统驱动。X4 用专门的 dummy 骨驱动眼球，
而本模型的眼球几何离它们较远，注视变化会把眼球甩出眼眶。

[b][size=3]6. 没有面部表情与口型同步[/size][/b]

X4 的程序化面部系统（facemods）与语音口型不适用。荧的表情是固定的。

[b][size=3]7. 近距离下衣服上的细深色线条会被放大[/size][/b]

这套模型的装饰边缘是几何体，映射到贴图集上很窄的一条（实测一个面跨 56 x 2 纹素），
渲染时两个纹素被放大成一条可见的带。正常距离看像滚边，特写下像条纹。这是源美术如此。

[b][size=3]8. 没有法线贴图，粗糙度是逐材质常数[/size][/b]

源是 MMD 模型：只有漫反射贴图，没有法线/粗糙度/金属通道。
法线用纯平 BC5 占位，粗糙度按材质手工设定。

[b][size=3]9. 比源模型粗糙[/size][/b]

X4 对 NPC 做实例化渲染，资产必须接近原版的约 5k 顶点。
本 mod 是 head 4813 / body 11325，原版是 4693 / 3601 —— 约 1.03 倍与 3.15 倍，
在预算内。这也是不再继续减面的原因（减面会重写 UV，而这些图集是按材质共用的，
UV 一偏就采到贴图的邻近部位）。

[line]

[b][size=4]兼容性[/size][/b]

[list]
[*]需要 X4: Foundations 9.00 或更新，以及《人类的摇篮》DLC
[*]不修改任何游戏文件，与绝大多数 mod 共存
[*]与 [b]Argon[/b] 外观替换 mod（萝丝、艾梅莉埃、2B 等）[b]不冲突[/b] ——
    那些改的是 Argon 池，本 mod 改的是 Terran macro
[*]与其他 [b]Terran[/b] 外观替换 mod [b]冲突[/b] —— 双方替换的是同一批 45 个 macro，
    后加载的生效
[*]存档安全，可随时启用/停用
[/list]

[line]

[b][size=4]AI 使用说明[/size][/b]

本 mod 背后的工具链 —— PMX 解析器、骨架重定向算法、贴图与打包管线、以及各类诊断脚本
—— 是在 AI 助手的协助下编写的。设计决策、实机测试与每一个问题的定位由作者完成。

工程保留了完整的逐轮工程日志，记录了发现并修复的每个 bug，也[b]如实记录了没修好的那个[/b]：
上面第 1 条的关节撕裂在日志里被标为未解决问题，连同"尝试过并已回退"的修法一起。

模型与贴图由源模型转换而来，[b]不是 AI 生成的[/b]。

[line]

[b][size=4]致谢[/size][/b]

[list]
[*]X4 Character Converter（作者 DiCrash / Orion）—— 没有这个导出器就没有这一切
    [url=https://www.nexusmods.com/x4foundations/mods/2152]nexusmods.com/x4foundations/mods/2152[/url]
[*]EGOSOFT 的 X Tools 与 X4 mod 文档
[/list]

（以上是制作本 mod 所用的工具，玩家无需安装。）

[line]

[b][size=4]版权[/size][/b]

《原神》是 miHoYo / HoYoverse 的商标，《X4：基石》是 EGOSOFT 的商标。
本作为非官方粉丝作品，仅供个人使用，不含上述任何一方的游戏资产。
荧的模型与贴图版权归 miHoYo，模型改造者为 [b]观海子[/b]。
